#!/usr/bin/env node
/**
 * 物理・化学・生物の基礎講座をスキャフォールド
 * content/academy/{physics,chemistry,biology}-exam/
 */
import fs from "node:fs";
import path from "node:path";
import { fileURLToPath } from "node:url";

const ROOT = path.join(path.dirname(fileURLToPath(import.meta.url)), "..");
const CONTENT = path.join(ROOT, "content/academy");

/** @typedef {{ id: string, chapter: string, title: string, minutes: number, goal: string, prereq: string[], points: string[], defName: string, defBody: string, steps: string[], exA: string, ansA: string, exB: string, ansB: string, drill: string, drillAns: string, traps: string[] }} UnitDef */

/** @type {Record<string, { subject: string, design: string, chapters: string[], units: UnitDef[] }>} */
const SUBJECTS = {
  "physics-exam": {
    subject: "高校物理（受験）",
    design: "力学・波動・電磁気・熱を受験の原子スキルに細分化。医学部でも出る標準〜やや難を意識。",
    chapters: ["力学の基礎", "運動と力", "エネルギーと運動量", "波動", "電気", "熱・気体"],
    units: [
      u("ME-PH-01", "力学の基礎", "物理量・単位・有効数字", 16, "SI単位と次元の感覚で式の正しさを検算できる", [], ["長さ・質量・時間の基本単位", "速度は m/s、力は N", "有効数字は計算の弱い方に合わせる"], "次元", "物理量の種類（長さL、質量M、時間T など）で式の整合を見る考え方。", ["量の定義を書く", "単位を揃える", "桁の妥当性を見る"], "速度 36 km/h を m/s に直せ。", "10 m/s", "力の単位 N を基本単位で表せ。", "kg·m/s²", "密度の単位を答えよ。", "kg/m³", ["単位の混在（km/h と m/s）", "有効数字を無闇に増やす"]),
      u("ME-PH-02", "力学の基礎", "位置・変位・速度・加速度", 18, "平均と瞬間、速度と加速度の違いを言える", ["ME-PH-01"], ["変位はベクトル、道のりはスカラー", "平均速度＝変位/時間", "加速度は速度の変化率"], "平均速度", "ある時間区間での変位をその時間で割ったもの。", ["グラフで傾きを読む", "符号（向き）を書く", "単位を確認"], "0→2 s で 0→10 m。平均速度は？", "5 m/s", "速度が一定のとき加速度は？", "0", "「速さ」と「速度」の違いを一句で。", "速さはスカラー、速度は向きを含むベクトル", ["平均と瞬間の混同", "向きを忘れる"]),
      u("ME-PH-03", "運動と力", "等速直線運動と等加速度直線運動", 18, "等加速度の3公式を使い分けられる", ["ME-PH-02"], ["v=v0+at", "x=v0t+½at²", "v²−v0²=2ax"], "等加速度", "加速度が一定の直線運動。", ["与えられた量を整理", "使う式を選ぶ", "向きを決めて符号"], "v0=0, a=2 m/s², t=3 s のときの v と x。", "v=6 m/s, x=9 m", "v0=10, v=0, a=−2 のときの x。", "x=25 m", "時間を含まない式は？", "v²−v0²=2ax", ["t と x の取り違え", "a の符号ミス"]),
      u("ME-PH-04", "運動と力", "自由落下・鉛直投射", 16, "g を用いた鉛直運動を立式できる", ["ME-PH-03"], ["下向き正なら a=+g", "上向き正なら a=−g", "最高点で v=0"], "重力加速度 g", "地表付近で約 9.8 m/s²（計算では 10 も可）。", ["正の向きを宣言", "最高点条件を使う", "往復時間を検算"], "高さ 45 m から静止落下（g=10）。着地速度は？", "30 m/s", "同じ条件の落下時間は？", "3 s", "上向きに 20 m/s で投げた最高点までの時間（g=10）。", "2 s", ["g の向きを途中で変える", "最高点で a=0 と思う"]),
      u("ME-PH-05", "運動と力", "力のつり合いと作用反作用", 16, "つり合いと作用反作用を混同しない", ["ME-PH-01"], ["つり合いは同一物体", "作用反作用は別物体・一直線・逆向き", "垂直抗力と重力はつり合いになり得るが作用反作用ではない"], "作用反作用", "物体AがBに力を及ぼすと、BはAに同じ大きさ逆向きの力を及ぼす。", ["力の矢印を物体ごとに描く", "つり合いか作用反作用かをラベル", "方程式を書く"], "机上の本：重力の反作用は？", "本が地球を引く力", "水平面で静止。垂直抗力 N と mg の関係。", "N=mg（つり合い）", "作用反作用の3条件を挙げよ。", "大きさ等しい・一直線・逆向き（別物体）", ["N と mg を作用反作用と言う", "力の抜け"]),
      u("ME-PH-06", "運動と力", "運動方程式 ma=F", 18, "合力を求めて加速度を出せる", ["ME-PH-05", "ME-PH-03"], ["ma=ΣF", "水平・鉛直に分解", "摩擦がある場合は向きに注意"], "運動方程式", "質量 m の物体の加速度 a は、働く力の合力 F に比例する。", ["自由物体図", "成分に分解", "a を求める"], "m=2 kg, F=10 N（摩擦なし）の a。", "5 m/s²", "同じ物体に反対向き 4 N。a は？", "3 m/s²", "単位 N は ma の単位と一致するか。", "一致する（kg·m/s²）", ["質量と重量の混同", "分解忘れ"]),
      u("ME-PH-07", "運動と力", "摩擦力（静止・運動）", 18, "静止摩擦と運動摩擦を使い分ける", ["ME-PH-06"], ["静止摩擦は 0〜μN", "運動摩擦は μ'N で一定向き", "動き出す直前が最大静止摩擦"], "最大静止摩擦力", "μN。これより大きい力が必要で動き出す。", ["垂直抗力 N を求める", "静止か運動か判定", "方程式"], "N=20 N, μ=0.3 の最大静止摩擦。", "6 N", "動いた後 μ'=0.2 の運動摩擦。", "4 N", "水平面で mg と N の関係（静止）。", "N=mg", ["μ と μ' の取り違え", "斜面で N=mg とする"]),
      u("ME-PH-08", "エネルギーと運動量", "仕事と運動エネルギー", 18, "W=Fx と K=½mv² を使える", ["ME-PH-06"], ["仕事は力×変位（向き一致成分）", "運動エネルギー定理", "仕事の単位 J"], "仕事", "力が物体を変位させるときにするエネルギー移動。W=F·s。", ["力と変位の向き", "仕事を計算", "ΔK と比較"], "F=10 N で 3 m 押す仕事。", "30 J", "m=2, v=3 の運動エネルギー。", "9 J", "仕事の単位を答えよ。", "J（＝N·m）", ["力と変位が垂直なのに仕事を数える", "½ を忘れる"]),
      u("ME-PH-09", "エネルギーと運動量", "力学的エネルギー保存", 18, "保存が使える条件と使い方を説明できる", ["ME-PH-08"], ["保存：重力・弾性のみ（非保存力の仕事0）", "K+U 一定", "摩擦があると減少"], "力学的エネルギー", "運動エネルギーと位置エネルギーの和。", ["保存可否を判定", "始点終点で式", "未知数を解く"], "高さ h から落下、速さは？", "√(2gh)", "摩擦ありのとき保存は？", "成り立たない（熱等に逃げる）", "ばねの U は？", "½kx²", ["摩擦ありで保存を使う", "基準面を途中変更"]),
      u("ME-PH-10", "エネルギーと運動量", "運動量と力積", 16, "mv と力積の関係を使える", ["ME-PH-06"], ["運動量 p=mv", "力積＝Δp", "衝突で保存（外力無視時）"], "力積", "力×時間が運動量変化に等しい。", ["前後の p を書く", "力積または保存", "跳ね返りに注意"], "m=2, v=3→0 の Δp。", "−6 kg·m/s", "一定力 5 N を 2 s。力積は？", "10 N·s", "単位 N·s は？", "kg·m/s と同じ次元", ["質量の抜け", "符号"]),
      u("ME-PH-11", "波動", "波の基本量（振幅・周期・波長）", 16, "f, T, λ, v の関係を使える", [], ["v=fλ", "T=1/f", "横波・縦波"], "波長 λ", "波の形が1回くり返す空間的な長さ。", ["与えられた量を整理", "v=fλ", "単位確認"], "f=50 Hz, λ=2 m の v。", "100 m/s", "T=0.02 s の f。", "50 Hz", "振幅の定義は？", "中心からの最大変位", ["f と T の逆数ミス", "振幅と波長の混同"]),
      u("ME-PH-12", "波動", "音波・弦・気柱の基礎", 18, "閉管・開管・弦の基本振動を説明できる", ["ME-PH-11"], ["弦：λ/2=L（基本）", "閉管：λ/4=L", "開管：λ/2=L"], "閉管の基本振動", "腹と節が端に来て λ=4L。", ["境界条件を確認", "λ を求める", "f=v/λ"], "閉管 L=0.25 m, v=340。基本 f は？", "340 Hz", "同じ L の開管基本 f は？", "680 Hz", "弦の両端は？", "節", ["開閉の取り違え", "倍振動を基本と混同"]),
      u("ME-PH-13", "電気", "電荷・電場・電位のイメージ", 18, "電場と電位の違いを言える", [], ["電場 E：力の場", "電位 V：エネルギーの観点", "等電位面では仕事0"], "電場", "単位電荷が受ける力。向きは＋電荷の力の向き。", ["力の向きを描く", "電位の高低", "仕事の有無"], "＋電荷は電位の高い方から低い方へ？", "高い→低いへ動くと電場が仕事", "等電位面に沿う移動の仕事。", "0", "電場の単位。", "N/C または V/m", ["電位と電場の混同", "向きの逆"]),
      u("ME-PH-14", "電気", "オームの法則と電力", 16, "V=IR, P=VI を使える", ["ME-PH-13"], ["直列は電流同じ", "並列は電圧同じ", "電力 P=I²R"], "オームの法則", "導体では電流は電圧に比例。V=IR。", ["回路を読む", "合成抵抗", "I または P"], "V=6, R=3 の I。", "2 A", "同じ条件の電力。", "12 W", "直列 2Ω と 4Ω の合成。", "6 Ω", ["並列を直列で計算", "P=VI と I²R の取り違え"]),
      u("ME-PH-15", "熱・気体", "温度・熱量・比熱", 16, "Q=mcΔT を使える", [], ["熱量 Q", "比熱 c", "熱平衡"], "比熱", "1 kg を 1 K 上げる熱量。", ["質量と ΔT", "Q=mcΔT", "単位"], "水 1 kg を 10 K 上げる（c=4.2×10³）。Q は？", "4.2×10⁴ J", "同じ熱で ΔT が大きいのは c が？", "小さい", "熱平衡とは？", "温度が等しくなり熱の移動が止まる状態", ["℃ と K の差を ΔT で間違える（差は同じ）", "質量の単位"]),
      u("ME-PH-16", "熱・気体", "理想気体の状態方程式", 18, "pV=nRT の使い方を説明できる", ["ME-PH-15"], ["絶対温度 T", "p, V, n, T の関係", "等温・等圧の変形"], "理想気体", "分子体積と分子間力を無視した気体模型。pV=nRT。", ["どの変数が一定か", "比を取る", "T はケルビン"], "T を2倍（V 一定）すると p は？", "2倍", "等温で V を半分にすると p は？", "2倍", "0℃ をケルビンで。", "273 K", ["℃ のまま代入", "n と V の混同"]),
    ],
  },
  "chemistry-exam": {
    subject: "高校化学（受験）",
    design: "理論・無機・有機を受験の手順単位に分割。計算と反応式の型を優先。",
    chapters: ["物質の構成", "化学量論", "気体・溶液", "反応と平衡", "無機", "有機"],
    units: [
      u("ME-CH-01", "物質の構成", "原子・分子・イオンと周期表", 16, "原子番号・質量数・イオンを説明できる", [], ["原子番号＝陽子数", "質量数＝陽子＋中性子", "イオンは電子の過不足"], "同位体", "原子番号が同じで質量数が異なる原子。", ["粒子数を数える", "イオン式", "周期表の族"], "⁶C の陽子・中性子（質量数12）。", "陽子6・中性子6", "Na⁺ の電子数（原子番号11）。", "10", "イオン結合のイメージは？", "陽イオンと陰イオンの静電引力", ["質量数と原子量の混同", "電子数の符号ミス"]),
      u("ME-CH-02", "物質の構成", "化学式と化学反応式のつくり方", 16, "係数で原子数を合わせられる", ["ME-CH-01"], ["左右の原子数一致", "係数は分子の前", "状態記号は後から"], "係数", "分子（式単位）の個数比を表す数。", ["未定係数を置く", "元素ごとに式", "整数化"], "H2+O2→H2O を整えよ。", "2H2+O2→2H2O", "係数 2 の意味は？", "分子が2個の割合", "反応式で保存されるのは？", "原子の種類と数（質量）", ["添字を変えてバランス", "係数と添字の混同"]),
      u("ME-CH-03", "化学量論", "モル・分子量・物質量", 18, "n=m/M, n=N/NA を使える", ["ME-CH-02"], ["1 mol＝NA 個", "モル質量 M", "気体の標準状態"], "物質量 n", "粒子の「個数のかたまり」を mol で測った量。", ["何が与えられたか", "公式選択", "単位"], "水 18 g は何 mol？", "1 mol", "CO2 のモル質量。", "44 g/mol", "NA の意味は？", "1 mol あたりの粒子数", ["g と mol の混同", "M の計算ミス"]),
      u("ME-CH-04", "化学量論", "反応式と量的関係（限界反応）", 18, "係数比で量を換算し限界反応を判定できる", ["ME-CH-03", "ME-CH-02"], ["係数比＝mol 比", "足りない方が限界", "収率"], "限界反応物", "先に使い切る反応物。生成量を決める。", ["両反応物の mol", "係数比で比較", "生成 mol"], "H2 2 mol と O2 2 mol から水は最大何 mol？", "2 mol（H2 が限界）", "係数 1:1 で A 3 mol, B 5 mol。限界は？", "A", "理論収量とは？", "限界反応物から計算した最大生成量", ["質量比を係数比と混同", "過剰側で計算"]),
      u("ME-CH-05", "気体・溶液", "気体の体積と状態方程式", 16, "標準状態と pV=nRT を使える", ["ME-CH-03"], ["標準で 22.4 L/mol", "T は K", "同じ条件なら V∝n"], "標準状態", "0℃・1 atm（定義に注意）。理想気体 1 mol が 22.4 L。", ["条件確認", "n を求める", "V または p"], "標準で O2 0.5 mol の体積。", "11.2 L", "T を℃から K へ。25℃は？", "298 K", "等温で n を2倍にすると V は？", "2倍", ["22.4 をいつでも使う", "℃ のまま"]),
      u("ME-CH-06", "気体・溶液", "濃度（mol/L・質量パーセント）", 16, "mol/L と希釈計算ができる", ["ME-CH-03"], ["c=n/V", "希釈前 c1V1=c2V2", "% は質量基準"], "モル濃度", "溶液 1 L 中の溶質の物質量。単位 mol/L。", ["溶質 n", "溶液体積", "希釈式"], "0.5 mol を 0.25 L に溶かした濃度。", "2.0 mol/L", "2 mol/L を 100 mL→200 mL に希釈後。", "1 mol/L", "質量パーセントの分母は？", "溶液全体の質量", ["mL を L に直し忘れ", "溶質と溶液の取り違え"]),
      u("ME-CH-07", "反応と平衡", "反応熱と熱化学方程式", 16, "発熱・吸熱と係数の意味を読める", ["ME-CH-02"], ["ΔH<0 発熱", "係数は mol 対応", "状態に注意"], "発熱反応", "系から熱が出る反応。ΔH は負。", ["熱化学式を読む", "1 mol あたりに換算", "符号"], "ΔH=−100 kJ は発熱か。", "発熱", "係数を2倍にすると ΔH は？", "2倍", "H2O(l) と (g) で ΔH が違う理由。", "気化熱の分", ["符号の逆", "係数無視"]),
      u("ME-CH-08", "反応と平衡", "化学平衡と平衡移動", 18, "ルシャトリエの原理で平衡のずれを説明できる", ["ME-CH-07"], ["平衡は正逆速度一致", "濃度・圧・温度で移動", "触媒は位置を変えない"], "ルシャトリエの原理", "平衡を乱す操作をすると、その効果を弱める向きに平衡が動く。", ["操作を特定", "どちらに動くか", "量の増減"], "発熱反応で温度を上げると平衡は？", "逆反応側へ", "気体の係数が減る反応で圧を上げると？", "生成系へ", "触媒を入れると平衡定数は？", "変わらない（到達が速いだけ）", ["K と速度の混同", "固体の濃度を圧と同様に扱う"]),
      u("ME-CH-09", "反応と平衡", "酸塩基と pH の基礎", 16, "H+ と pH の関係を使える", ["ME-CH-06"], ["pH=−log[H+]", "中和は H+ と OH−", "強酸の近似"], "pH", "水素イオン濃度の指標。小さいほど酸性。", ["[H+] を求める", "pH 計算", "中和の量関係"], "[H+]=1×10−3 の pH。", "3", "pH=7 は？", "中性（25℃付近）", "NaOH と HCl の中和生成物。", "NaCl と水", ["log の符号", "弱酸を強酸と同じに近似しすぎ"]),
      u("ME-CH-10", "反応と平衡", "酸化還元と半反応", 18, "酸化数の増減で酸化還元を判定できる", ["ME-CH-02"], ["酸化＝酸化数増加", "還元＝減少", "電子の授受"], "酸化数", "形式的な電荷。酸化還元の収支を追う道具。", ["酸化数を振る", "変化を見る", "電子数を合わせる"], "Fe²⁺→Fe³⁺ は酸化か還元か。", "酸化", "O2 中の O の酸化数。", "0", "酸化剤は自分は？", "還元される", ["酸化還元の用語逆転", "多原子イオンの酸化数ルール無視"]),
      u("ME-CH-11", "無機", "典型元素の性質（Na, Cl, ハロゲン等）", 16, "族の性質の傾向を説明できる", ["ME-CH-01"], ["アルカリ金属は陽イオンになりやすい", "ハロゲンは陰イオン", "周期で半径・イオン化傾向"], "イオン化エネルギー", "原子から電子を取り除くのに必要なエネルギー。", ["族の特徴", "反応性の傾向", "代表反応"], "Na と水の反応で出る気体。", "H2", "ハロゲンで反応性が大きいのは？", "F（上の方）", "貴ガスが安定な理由のイメージ。", "閉殻", ["ハロゲンの反応性順の逆", "単体とイオンの混同"]),
      u("ME-CH-12", "無機", "遷移元素と錯イオンの入り口", 16, "遷移元素の特徴を挙げられる", ["ME-CH-11"], ["複数の酸化数", "有色イオン", "錯イオン"], "錯イオン", "金属イオンに配位子が配位したイオン。", ["中心金属", "配位子", "色・触媒"], "Cu²⁺ 水溶液の色のイメージ。", "青", "配位子の例。", "NH3, H2O, Cl− など", "遷移元素が触媒になりやすい理由の一端。", "酸化数を変えやすい", ["主族と同じ扱い", "錯イオンの電荷の計算ミス"]),
      u("ME-CH-13", "有機", "炭化水素の分類と命名の入口", 16, "アルカン・アルケン・アルキンを区別できる", [], ["飽和／不飽和", "一般式", "付加と置換"], "アルケン", "C=C を持つ炭化水素。一般式 CnH2n。", ["結合を見る", "一般式", "反応の型"], "C2H4 の分類。", "アルケン", "メタンの反応は主に？", "置換", "ベンゼンは？", "芳香族（特殊な安定性）", ["アルケンとアルカンの一般式混同", "付加と置換の取り違え"]),
      u("ME-CH-14", "有機", "官能基とアルコール・カルボン酸", 18, "官能基から性質を予測できる", ["ME-CH-13"], ["−OH アルコール", "−COOH 酸", "エステル化"], "官能基", "化合物の性質を特徴づける原子団。", ["官能基を見つける", "反応を選ぶ", "生成物"], "エタノールの官能基。", "ヒドロキシ基 −OH", "酢酸の官能基。", "カルボキシ基 −COOH", "アルコール＋酸→？", "エステル＋水", ["アルデヒドとケトンの混同", "酸化の条件無視"]),
      u("ME-CH-15", "有機", "油脂・アミノ酸・糖の受験ポイント", 16, "生体関連有機の型を説明できる", ["ME-CH-14"], ["油脂はエステル", "アミノ酸は両性", "糖の還元性"], "ペプチド結合", "アミノ酸どうしの −CO−NH− 結合。", ["構造の特徴", "反応名", "検出"], "油脂のけん化でできるものは？", "石けん（塩）とグリセリン", "アミノ酸が両性な理由。", "−COOH と −NH2 を持つ", "還元性糖の例。", "グルコースなど", ["けん化と硬化の混同", "必須アミノ酸の暗記だけに逃げる"]),
    ],
  },
  "biology-exam": {
    subject: "高校生物（受験）",
    design: "細胞・遺伝・体内環境・生態を医学部受験で問われる説明型に分割。用語と因果を重視。",
    chapters: ["細胞", "代謝", "遺伝", "体内環境", "動物の反応", "植物・生態"],
    units: [
      u("ME-BI-01", "細胞", "細胞の構造と細胞小器官", 16, "核・ミトコンドリア・リボソーム等の役割を言える", [], ["原核と真核", "動物と植物の違い", "分業"], "ミトコンドリア", "呼吸の場。ATP を多くつくる。", ["構造を列挙", "機能を対応", "動植物差"], "タンパク質合成の場は？", "リボソーム", "植物だけにある主な構造。", "細胞壁・葉緑体など", "核の役割。", "遺伝情報の保持・発現制御", ["小器官の機能入れ替え", "原核に核ありとする"]),
      u("ME-BI-02", "細胞", "細胞膜と輸送（拡散・浸透・能動輸送）", 18, "受動輸送と能動輸送を区別できる", ["ME-BI-01"], ["濃度勾配", "浸透", "ATP を使う能動"], "能動輸送", "濃度勾配に逆らう物質輸送。エネルギーが必要。", ["勾配の向き", "エネルギー要否", "例"], "酸素の細胞への入り方の典型。", "拡散（受動）", "Na+/K+ ポンプは？", "能動輸送", "高張液に入れた細胞は？", "縮む（動物）／原形質分離（植物）", ["浸透と拡散の混同", "能動を受動と言う"]),
      u("ME-BI-03", "代謝", "酵素の性質", 16, "基質特異性・最適条件を説明できる", [], ["触媒", "基質特異性", "温度・pH"], "酵素", "生体内の触媒。主にタンパク質。", ["条件を変える", "活性の変化", "阻害"], "高温で活性が落ちる主な理由。", "タンパク質の変性", "基質が決まっている性質。", "基質特異性", "競合阻害のイメージ。", "活性部位を奪い合う", ["酵素が反応で消費されると思う", "最適温度を人体体温だけと決めつける"]),
      u("ME-BI-04", "代謝", "呼吸と光合成の全体像", 18, "呼吸と光合成の関係を物質・エネルギーで説明できる", ["ME-BI-03", "ME-BI-01"], ["呼吸：有機物→ATP", "光合成：光→有機物", "ミトコンドリアと葉緑体"], "ATP", "細胞内のエネルギーの一時的な運び手。", ["式の概略", "場所", "相互関係"], "呼吸の主な場。", "ミトコンドリア", "光合成の場。", "葉緑体", "光合成の材料の代表。", "CO2 と H2O（＋光）", ["呼吸と光合成を無関係と捉える", "酸素の出入りの逆"]),
      u("ME-BI-05", "遺伝", "DNA・遺伝子・染色体", 16, "遺伝情報の階層を説明できる", ["ME-BI-01"], ["DNA→遺伝子→染色体", "塩基対", "複製"], "遺伝子", "形質に関わる DNA の一定領域。", ["用語の対応", "複製の相補性", "体細胞と生殖細胞"], "相補的な塩基対。", "A-T, G-C", "ヒトの体細胞の染色体数の目安。", "46本（23対）", "半保存的複製とは？", "古い鎖と新しい鎖がペア", ["染色体数と遺伝子数の混同", "RNA と DNA の取り違え"]),
      u("ME-BI-06", "遺伝", "メンデルの法則と遺伝の基本計算", 18, "分離の法則で表現型比を求められる", ["ME-BI-05"], ["優性・劣性", "ホモ・ヘテロ", "検定交雑"], "分離の法則", "対立遺伝子は配偶子形成時に分かれる。", ["親の遺伝子型", "配偶子", "Punnett"], "Aa × Aa の表現型比（完全優性）。", "3:1", "Aa × aa は？", "1:1", "検定交雑の相手。", "劣性ホモ", ["表現型と遺伝子型の混同", "独立の法則を一遺伝子に誤用"]),
      u("ME-BI-07", "遺伝", "血統図と伴性遺伝の入口", 16, "血統図の記号と伴性の特徴を読める", ["ME-BI-06"], ["□男性○女性", "塗りつぶし発症", "X 連鎖の伝わり方"], "伴性遺伝", "性染色体上の遺伝子による遺伝。", ["記号を読む", "世代を追う", "男女差"], "X 連鎖劣性で男性に多い理由の一端。", "X が1本なので隠れにくい", "両親が保因者の常染色体劣性。子の発症率。", "1/4", "血統図で世代を表す横の並び。", "同一世代", ["常染色体と性染色体の取り違え", "保因者を発症と読む"]),
      u("ME-BI-08", "体内環境", "体液と恒常性", 16, "内部環境の一定維持を説明できる", [], ["体液の区分", "恒常性", "フィードバック"], "恒常性（ホメオスタシス）", "体内環境を一定範囲に保つ性質。", ["何が一定か", "センサーと応答", "例"], "血糖を下げるホルモンの代表。", "インスリン", "フィードバックの基本。", "結果が原因を抑える向き", "体液の例。", "血液・リンパ・組織液", ["恒常性＝完全固定と思う", "ホルモン名の丸暗記のみ"]),
      u("ME-BI-09", "体内環境", "免疫の基本（自然・獲得）", 18, "自然免疫と獲得免疫を区別できる", ["ME-BI-08"], ["自然は非特異的", "獲得は特異的・記憶", "抗体"], "抗体", "抗原に特異的に結合するタンパク質。", ["経路を分ける", "細胞の役割", "二次応答"], "二次免疫応答の特徴。", "速く強い", "食作用を行う細胞の例。", "マクロファージなど", "ワクチンの原理の一端。", "記憶細胞を作らせる", ["抗体と抗生物質の混同", "自然と獲得の逆転"]),
      u("ME-BI-10", "動物の反応", "神経と興奮伝導", 16, "活動電位とシナプスの概要を言える", ["ME-BI-01"], ["静止電位", "活動電位", "シナプス伝達"], "シナプス", "ニューロン間の接合部。化学伝達が典型。", ["方向性", "伝達物質", "反射"], "興奮の伝導方向（軸索）。", "細胞体側→終末", "反射の特徴。", "速い・無意識的など", "髄鞘があると伝導は？", "跳躍伝導で速い", ["伝導と伝達の用語混同", "方向の逆"]),
      u("ME-BI-11", "動物の反応", "受容器と効果器・ホルモン", 16, "神経系と内分泌の役割分担を説明できる", ["ME-BI-10", "ME-BI-08"], ["受容器→中枢→効果器", "ホルモンは血液経由", "速さと持続"], "ホルモン", "内分泌腺から血液へ出る情報物質。", ["経路を描く", "例", "神経との比較"], "血糖調節で神経系とホルモンは？", "協調して働く", "効果器の例。", "筋肉・腺", "内分泌が神経より一般に遅い理由の一端。", "血液循環に依存", ["外分泌と内分泌の混同", "一例だけで一般化しすぎ"]),
      u("ME-BI-12", "植物・生態", "植物ホルモンと環境応答", 16, "オーキシン等の働きを説明できる", [], ["屈光性", "成長", "環境応答"], "オーキシン", "成長を調節する植物ホルモンの代表。", ["刺激", "分布", "応答"], "屈光性で先端の役割。", "光を受容し側へ偏る", "エチレンのイメージ。", "成熟・脱落など", "光発芽種子に重要な光。", "赤色／遠赤色など（フィトクロム）", ["動物ホルモンと同じ仕組みと思う", "名称だけの暗記"]),
      u("ME-BI-13", "植物・生態", "生態系の物質循環とエネルギー", 18, "食物網と炭素循環の骨格を説明できる", ["ME-BI-04"], ["生産者・消費者・分解者", "エネルギーは一方通行", "物質は循環"], "生産者", "無機物から有機物をつくる生物。主に植物。", ["役割を割り当て", "矢印の意味", "人間活動"], "エネルギーの最終的な行き先のイメージ。", "熱として散逸", "分解者の役割。", "有機物を無機物へ", "炭素が大気に戻る過程の例。", "呼吸・燃焼", ["物質とエネルギーを同じ循環で描く", "食物連鎖を一本だけと捉える"]),
      u("ME-BI-14", "植物・生態", "個体群と生物間関係", 16, "競争・捕食・共生を区別できる", ["ME-BI-13"], ["個体数変化", "密度効果", "関係の型"], "競争", "同じ資源を奪い合う関係。", ["関係を分類", "例", "密度"], "相利共生の例の方向性。", "双方に利益", "捕食者−被捕食者。", "一方が他を食べる", "環境収容力とは？", "その環境で維持できる最大個体数の目安", ["共生の種類の混同", "個体と群れの用語粗雑"]),
      u("ME-BI-15", "遺伝", "タンパク質合成（転写・翻訳）の入口", 18, "転写と翻訳の場所・役割を言える", ["ME-BI-05", "ME-BI-01"], ["転写：DNA→mRNA", "翻訳：リボソーム", "コドン"], "コドン", "mRNA 上の3塩基でアミノ酸を指定する単位。", ["場所", "分子", "流れ"], "転写の場（真核）。", "核", "翻訳の場。", "リボソーム（細胞質）", "tRNA の役割。", "アミノ酸を運ぶ", ["転写翻訳の順序逆", "DNA が直接リボソームへ"]),
    ],
  },
};

function u(id, chapter, title, minutes, goal, prereq, points, defName, defBody, steps, exA, ansA, exB, ansB, drill, drillAns, traps) {
  return { id, chapter, title, minutes, goal, prereq, points, defName, defBody, steps, exA, ansA, exB, ansB, drill, drillAns, traps };
}

function lessonMd(subjectLabel, unit, nextId) {
  const points = unit.points.map((p) => `- ${p}`).join("\n");
  const steps = unit.steps.map((s, i) => `${i + 1}. ${s}`).join("\n");
  const traps = unit.traps.map((t) => `- ${t}`).join("\n");
  const prereq = unit.prereq.length ? unit.prereq.join(", ") : "なし";
  return `# ${unit.id} ${unit.title}

- ブランド: **Medvance**（医学部・難関大受験）
- 科目: ${subjectLabel}
- 章: ${unit.chapter}
- 目安: **${unit.minutes}分**
- 前提: ${prereq}
- 次: ${nextId || "（章末）"}

## 今日のゴール
${unit.goal}

## 受験での位置づけ
医学部・難関大では用語の定義と手順の再現がそのまま得点になる。この単元はその「型」を固定する回。

## 前提チェック
${unit.prereq.length ? unit.prereq.map((p) => `- ${p}`).join("\n") : "- 入口ユニット（この回から開始してよい）"}

## この回のポイント
${points}

## 定義・用語
### ${unit.defName}
${unit.defBody}

## 核となる手順
${steps}

## 例題A（標準）
${unit.exA}

### 解答A
- ${unit.ansA}

## 例題B（受験）
${unit.exB}

### 解答B
- ${unit.ansB}

## 演習
${unit.drill}

### 解答
${unit.drillAns}

## 落とし穴（減点パターン）
${traps}

## まとめ
- ゴールを自分の言葉で言えるか
- 例題A・Bを何も見ずに再現できるか

## 次へ
${nextId ? `\`${nextId}\`` : "次の章へ進む前に、この章の用語を一通り説明できるか確認する。"}

---
© Medvance · 無断転載禁止
`;
}

function storyboardMd(unit) {
  return `# 台本 ${unit.id} ${unit.title}

1. タイトル・ゴール
2. 受験での位置づけ
3. 前提チェック
4. ポイント提示
5. 定義
6. 手順
7. 例題A
8. 解答A
9. 例題B
10. 解答B
11. 演習
12. 落とし穴・まとめ

ナレーションは lesson 本文に準拠。台本外の例を増やさない。
`;
}

function quizJson(unit) {
  return {
    unit_id: unit.id,
    title: unit.title,
    pass_score: 4,
    questions: [
      { id: "q1", type: "short", prompt: "ゴールは？", answer: unit.goal },
      { id: "q2", type: "short", prompt: "例題Aの要点は？", answer: unit.ansA },
      {
        id: "q3",
        type: "mc",
        prompt: "落とし穴として近いものは？",
        choices: [unit.traps[0], "勘で答える", "公式をすべて無視する", "問題文を読まない"],
        answer: unit.traps[0],
      },
      { id: "q4", type: "short", prompt: "手順の第1ステップは？", answer: unit.steps[0] },
      { id: "q5", type: "short", prompt: "演習の答えは？", answer: unit.drillAns },
    ],
  };
}

function writeSubject(dirName, cfg) {
  const base = path.join(CONTENT, dirName);
  for (const sub of ["lessons", "storyboard", "slides", "quiz"]) {
    fs.mkdirSync(path.join(base, sub), { recursive: true });
  }
  const units = cfg.units;
  const indexUnits = units.map((unit, i) => {
    const next = units[i + 1]?.id;
    fs.writeFileSync(path.join(base, "lessons", `${unit.id}.md`), lessonMd(cfg.subject, unit, next));
    fs.writeFileSync(path.join(base, "storyboard", `${unit.id}.md`), storyboardMd(unit));
    fs.writeFileSync(path.join(base, "slides", `${unit.id}.md`), `# スライド ${unit.id}\n\n${unit.points.map((p) => `- ${p}`).join("\n")}\n`);
    fs.writeFileSync(path.join(base, "quiz", `${unit.id}.json`), JSON.stringify(quizJson(unit), null, 2));
    return {
      id: unit.id,
      chapter: unit.chapter,
      title: unit.title,
      minutes: unit.minutes,
      goal: unit.goal,
      prereq: unit.prereq,
      status: "full",
      file: `lessons/${unit.id}.md`,
      storyboard: `storyboard/${unit.id}.md`,
      slides: `slides/${unit.id}.md`,
      quiz: `quiz/${unit.id}.json`,
    };
  });

  const index = {
    subject: cfg.subject,
    track: "exam",
    trackLabel: "基礎講座",
    brand: "Medvance",
    unit_count: indexUnits.length,
    full_count: indexUnits.length,
    outline_count: 0,
    design: cfg.design,
    chapters: cfg.chapters,
    units: indexUnits,
  };
  fs.writeFileSync(path.join(base, "index.json"), JSON.stringify(index, null, 2));
  fs.writeFileSync(
    path.join(base, "README.md"),
    `# ${cfg.subject}\n\nMedvance 基礎講座。ユニット数: ${indexUnits.length}\n`,
  );
  console.log("wrote", dirName, indexUnits.length, "units");
  return indexUnits.length;
}

// generate
let totalNew = 0;
for (const [dir, cfg] of Object.entries(SUBJECTS)) {
  totalNew += writeSubject(dir, cfg);
}

// update catalog.json
const catalogPath = path.join(CONTENT, "catalog.json");
const catalog = JSON.parse(fs.readFileSync(catalogPath, "utf8"));

const scienceSubjects = [
  {
    id: "physics-exam",
    path: "curriculum/physics-exam",
    subject: "高校物理（受験）",
    unit_count: SUBJECTS["physics-exam"].units.length,
    full_count: SUBJECTS["physics-exam"].units.length,
    outline_count: 0,
    trackLabel: "基礎講座",
  },
  {
    id: "chemistry-exam",
    path: "curriculum/chemistry-exam",
    subject: "高校化学（受験）",
    unit_count: SUBJECTS["chemistry-exam"].units.length,
    full_count: SUBJECTS["chemistry-exam"].units.length,
    outline_count: 0,
    trackLabel: "基礎講座",
  },
  {
    id: "biology-exam",
    path: "curriculum/biology-exam",
    subject: "高校生物（受験）",
    unit_count: SUBJECTS["biology-exam"].units.length,
    full_count: SUBJECTS["biology-exam"].units.length,
    outline_count: 0,
    trackLabel: "基礎講座",
  },
];

const foundation = catalog.tracks.find((t) => t.id === "foundation");
if (foundation) {
  // remove if re-run
  foundation.subjects = foundation.subjects.filter(
    (s) => !["physics-exam", "chemistry-exam", "biology-exam"].includes(s.id),
  );
  foundation.subjects.push(...scienceSubjects);
}

catalog.subjects = catalog.subjects.filter(
  (s) => !["physics-exam", "chemistry-exam", "biology-exam"].includes(s.id),
);
catalog.subjects.push(...scienceSubjects);

// recount foundation
const foundCount = foundation.subjects.reduce((a, s) => a + (s.full_count || s.unit_count || 0), 0);
const advCount = (catalog.tracks.find((t) => t.id === "advanced")?.subjects || []).reduce(
  (a, s) => a + (s.full_count || s.unit_count || 0),
  0,
);
const eliteCount = (catalog.tracks.find((t) => t.id === "elite")?.subjects || []).reduce(
  (a, s) => a + (s.full_count || s.unit_count || 0),
  0,
);
catalog.totals = {
  foundation_units: foundCount,
  advanced_units: advCount,
  elite_units: eliteCount,
  foundation_full: foundCount,
  advanced_full: advCount,
  elite_full: eliteCount,
  all_units: foundCount + advCount + eliteCount,
  all_full: foundCount + advCount + eliteCount,
};

fs.writeFileSync(catalogPath, JSON.stringify(catalog, null, 2));
console.log("catalog updated totals", catalog.totals);
console.log("new science units", totalNew);
